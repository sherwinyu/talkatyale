<%= form_for(@test_obj) do |f| %>
  <% if @test_obj.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_obj.errors.count, "error") %> prohibited this test_obj from being saved:</h2>

      <ul>
      <% @test_obj.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
