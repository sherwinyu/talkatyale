<%= form_for(@test_obj2) do |f| %>
  <% if @test_obj2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@test_obj2.errors.count, "error") %> prohibited this test_obj2 from being saved:</h2>

      <ul>
      <% @test_obj2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
